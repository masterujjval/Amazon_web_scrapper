#!/bin/bash

# adding the script to /usr/local/bin so that script can be executed using command only 

sudo cp amazon /usr/local/bin
sudo cp amazon_web.py /usr/local/bin


echo "Fire the amazon bot with 'amazon' command"
